import React from 'react';

export default function UpNext() {
  return (
    <div>
      <p>Up Next Component</p>
    </div>
  )
}

//container that will dynamically render server name (next in rotation)